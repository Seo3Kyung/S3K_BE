<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.s3k.backend.home.mapper.StopoverMapper">

  <select id="findAllByWalkingPathId" resultType="com.s3k.backend.home.entity.Stopover">
    SELECT
        stopover_comment as stopoverComment
        , stopover_category as stopoverCategory
        , address
    FROM stopover
    where walking_path_id = #{walkingPathId}
  </select>
</mapper>
